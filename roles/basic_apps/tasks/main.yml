---
- name: Desktop 사용자에게 sudo 권한 부여
  include_tasks: tasks/task_allow_sudoer.yml

- name: update and upgrade apt
  become: true
  apt: upgrade=yes update_cache=yes cache_valid_time=3600

- name: install some terminal applications
  include_tasks: tasks/task_tools.yml

- name: copy .bashrc
  include_tasks: tasks/task_bashrc.yml

- name: install D2Coding fonts
  include_role: name=font_d2coding

# - name: update desktop interface
#   block:
#     - name: copy disktop interface configuration to local temporary directory
#       copy: src=files/desktop_interface.dconf dest={{ tmp_dir }}/desktop_interface.dconf

#     - name: load desktop interface configuration
#       shell: "dconf load /org/gnome/desktop/interface/ < {{ tmp_dir }}/desktop_interface.dconf"

#     - name: remove temporary configuration file
#       file: path={{ tmp_dir }}/desktop_interface.dconf state=absent

- name: install vim
  include_role: name=vim

- name: generate SSH key
  include_role: name=generate_ssh_key
