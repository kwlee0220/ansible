---
# MoveIt 2 빌드를 위한 기본 변수
#
# ROS 2 배포판(ros2_distro)은 의존 role 인 ros2 가 제공한다.
ws_path: "{{ ansible_env.HOME }}/local/ros2/ws_moveit2"

# moveit2_tutorials 저장소의 브랜치 이름은 ROS 2 배포판 이름과 같다.
moveit_branch: "{{ ros2_distro }}"

# 메모리 부족 방지를 위한 병렬 빌드 제한 (시스템 사양에 따라 조절)
max_parallel_workers: 2
