---
# 0) Fail-fast validation
- name: Validate role variables
  ansible.builtin.include_tasks: validate.yml

# 1) Ubuntu deps (22.04 / 24.04 공통)
- name: Install Ubuntu dependencies
  ansible.builtin.include_tasks: deps_ubuntu.yml

# 2) Install Poetry
- name: Install Poetry
  ansible.builtin.include_tasks: install.yml

# 3) Configure bash init files
- name: Configure bash init files (.bash_profile/.bashrc)
  ansible.builtin.include_tasks: shell_init.yml
