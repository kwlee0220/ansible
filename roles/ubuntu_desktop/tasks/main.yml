---
- name: install ubuntu-desktop and gnome
  become: true
  package:
    name:
      - ubuntu-desktop
      # - gnome

- name: Set dconf values
  become: false
  dconf:
    key: "{{ item.key }}"
    value: "{{ item.value }}"
  with_items: "{{ desktop_confs }}"