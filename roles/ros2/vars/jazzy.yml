---
# ROS 2 Jazzy (Ubuntu 24.04 noble) 전용 변수
#
# 공통 목록(defaults/main.yml 의 ros2_packages)과 이름이 같은 패키지는 여기
# 적을 필요가 없다. Jazzy 에만 존재하거나 이름이 다른 패키지만 나열한다.
#
# 주의: 시뮬레이터는 Humble(Gazebo Classic) 과 Jazzy(Gazebo Harmonic / ros_gz)
# 가 서로 다른 소프트웨어이므로 이 role 이 아니라 별도 role 에서 다룬다.

ros2_packages_extra: []
