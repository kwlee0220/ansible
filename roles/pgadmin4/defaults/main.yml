---
# Default variables for pgAdmin4 installation
#
# 이 role 은 데스크톱 버전만 설치한다. upstream APT 저장소에서 web 모드용
# pgadmin4 / pgadmin4-web 패키지가 없어졌고, 남아 있는 것은 pgadmin4-desktop 과
# 그 의존성인 pgadmin4-server 뿐이다.
# 따라서 web 모드용 관리자 계정(email/password) 설정도 더 이상 하지 않는다.
pgadmin4_package: pgadmin4-desktop
