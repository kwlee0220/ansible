---
- name: Install Kubernetes
  hosts: k8s

  vars:
    set_hostname: true

  roles:
    - role: set-hostname
      when: set_hostname

- import_playbook: playbooks/install_containerd.yml
- import_playbook: playbooks/install_k8s_hosts.yml
- import_playbook: playbooks/initialize_k8s.yml
